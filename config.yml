system:
  device: "cuda"
  num_workers: 4
  pin_memory: true

data:
  csv_path: "dataset.csv"
  images_dir: "images"
  img_size: 224
  val_fold: 0

model:
  name: "mobileclip_s2"
  head_hidden_dim: 256  

train:
  batch_size: 64
  lr_backbone: 0.0      
  lr_head: 1.0e-3       
  epochs: 15
  weight_decay: 1.0e-4  
  grad_clip: 1.0
  seed: 42